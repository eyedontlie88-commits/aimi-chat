# Database (PostgreSQL for production, local dev)
# For Vercel Postgres / Neon / Supabase:
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require&pgbouncer=true"
DIRECT_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Schema Selection (Dev/Prod Split using same database)
# APP_ENV controls schema enforcement (not NODE_ENV!)
# - Production service: APP_ENV=production, APP_SCHEMA=public (enforced)
# - Dev service: APP_ENV=dev, APP_SCHEMA=dev (default)
# Both services can run with NODE_ENV=production if needed
APP_ENV=dev
APP_SCHEMA=dev

NODE_ENV=development

# LLM Configuration
# Valid providers: "silicon" | "gemini"
# (DeepSeek & Moonshot have been disabled in code)
LLM_DEFAULT_PROVIDER=gemini
LLM_DEFAULT_MODEL=gemini-1.5-flash

# Fallback Strategy
LLM_FALLBACK_PROVIDERS=silicon
LLM_ENABLE_FALLBACK=true

# SiliconFlow
SILICON_BASE_URL=https://api.siliconflow.cn/v1
SILICON_API_KEY=sk-...
SILICON_DEFAULT_MODEL=Qwen/Qwen2.5-7B-Instruct

# Gemini (Google AI)
GEMINI_API_KEY=your_api_key_here
GEMINI_DEFAULT_MODEL=gemini-1.5-flash

# ============================================================
# DEPRECATED: DeepSeek & Moonshot providers
# These have been removed from the codebase.
# Keep for reference only if you want to re-enable them later.
# ============================================================
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
# DEEPSEEK_API_KEY=sk-...
# DEEPSEEK_DEFAULT_MODEL=deepseek-chat

# MOONSHOT_BASE_URL=https://api.moonshot.cn/v1
# MOONSHOT_API_KEY=sk-...
# MOONSHOT_DEFAULT_MODEL=moonshot-v1-32k

# ============================================================
# FIREBASE AUTH
# ============================================================

# Firebase Client SDK (exposed to browser - NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdef
# Web Client ID for Google Sign-In (from Google Cloud Console OAuth 2.0 Client - Web application type)
NEXT_PUBLIC_FIREBASE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com

# Firebase Admin SDK (server-only, JSON string)
# Copy from Firebase Console > Project Settings > Service Accounts > Generate new private key
# Paste entire JSON as single line, escape quotes if needed
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Auth Configuration
# Set to true to require authentication on all API routes (disable anonymous mode)
REQUIRE_AUTH=false

# Admin secret for dev/diagnostic endpoints (non-production only)
# Used by:
# - POST /api/admin/set-dev (header: x-dev-admin-secret)
# - GET /api/admin/llm-status (header: x-admin-secret)
# Required in production, optional in dev mode (APP_ENV=dev)
DEV_ADMIN_SECRET=random-secret-change-this
